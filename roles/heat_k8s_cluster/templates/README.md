templates/ — Templates for `heat_k8s_cluster`

Contains Heat templates and helper templates used to render the Heat stack and worker node manifest. These are rendered with Ansible `template` and used to create or update the Heat stack.
