defaults/ — Default variables for `heat_k8s_cluster`

Contains `main.yml` which sets sensible defaults for image names, flavors, keypair name, node counts, and other role-specific defaults. Override these values in `vars/cluster.yml` or in playbook variables.
