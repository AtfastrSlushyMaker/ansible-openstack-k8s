[defaults]
inventory = /etc/ansible/inventory.ini
host_key_checking = False
timeout = 30
gathering = smart

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
