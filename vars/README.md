vars/ — Configuration variables

Contains YAML files used to configure the deployment:

- `cluster.yml` — cluster configuration (images, flavors, network, counts)
- `credentials.yml` — local credentials (not committed by default)

Edit `cluster.yml` to adjust the cluster settings for your environment. Keep sensitive credentials private.
